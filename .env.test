# Configuração dos testes de integração (npm run test:integration).
# Aponta para o serviço db-test do docker-compose (profile "test").
# Não contém segredos: são credenciais de um container local descartável.
NODE_ENV=test
DB_HOST=localhost
DB_PORT=5434
DB_USER=adotapet
DB_PASSWORD=adotapet123
DB_NAME=adotapet_test
SESSION_SECRET=test-secret

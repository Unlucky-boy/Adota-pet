<%- include('../layout-header') %>

<section class="admin-section">
  <div class="container">
    <div class="admin-header">
      <h1><i data-lucide="clipboard-list" aria-hidden="true"></i> Solicitações de Adoção</h1>
    </div>

    <% if (adoptions.length === 0) { %>
      <div class="empty-state">
        <span class="empty-state__icon" aria-hidden="true"><i data-lucide="inbox"></i></span>
        <h3>Nenhuma solicitação recebida</h3>
        <p>Quando alguém solicitar a adoção de um pet, aparecerá aqui.</p>
      </div>
    <% } else { %>
      <div class="table-container">
        <table class="table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Pet</th>
              <th>Solicitante</th>
              <th>Contato</th>
              <th>Data</th>
              <th>Status</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            <% adoptions.forEach(function(adoption) { %>
              <tr>
                <td>#<%= adoption.id %></td>
                <td>
                  <strong><%= adoption.pet_name %></strong>
                  <br><small><i data-lucide="<%= adoption.pet_species === 'dog' ? 'dog' : 'cat' %>" aria-hidden="true"></i></small>
                </td>
                <td>
                  <strong><%= adoption.adopter_name %></strong>
                  <% if (adoption.message) { %>
                    <br><small title="<%= adoption.message %>">💬 "<%= adoption.message.substring(0, 50) %><%= adoption.message.length > 50 ? '...' : '' %>"</small>
                  <% } %>
                </td>
                <td>
                  <i data-lucide="mail" aria-hidden="true"></i> <%= adoption.adopter_email %>
                  <% if (adoption.adopter_phone) { %>
                    <br><i data-lucide="phone" aria-hidden="true"></i> <%= adoption.adopter_phone %>
                  <% } %>
                </td>
                <td><%= new Date(adoption.created_at).toLocaleDateString('pt-BR') %></td>
                <td>
                  <span class="status-badge status-badge--<%= adoption.status %>">
                    <%= adoption.status === 'pending' ? 'Pendente' : adoption.status === 'approved' ? 'Aprovada' : adoption.status === 'completed' ? 'Adotada' : 'Rejeitada' %>
                  </span>
                </td>
                <td class="table__actions">
                  <% if (adoption.status === 'pending') { %>
                    <form method="POST" action="/admin/adoptions/<%= adoption.id %>/status" style="display:inline">
                      <input type="hidden" name="status" value="approved">
                      <button type="submit" class="btn btn--success btn--sm"
                              onclick="return confirm('Aprovar esta solicitação?')"><i data-lucide="check"></i> Aprovar</button>
                    </form>
                    <form method="POST" action="/admin/adoptions/<%= adoption.id %>/status" style="display:inline">
                      <input type="hidden" name="status" value="rejected">
                      <button type="submit" class="btn btn--danger btn--sm"
                              onclick="return confirm('Rejeitar esta solicitação?')"><i data-lucide="x"></i> Rejeitar</button>
                    </form>
                  <% } else { %>
                    <form method="POST" action="/admin/adoptions/<%= adoption.id %>/status" style="display:inline">
                      <input type="hidden" name="status" value="pending">
                      <button type="submit" class="btn btn--outline btn--sm"><i data-lucide="undo-2"></i> Voltar p/ Pendente</button>
                    </form>
                  <% } %>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</section>

<%- include('../layout-footer') %>

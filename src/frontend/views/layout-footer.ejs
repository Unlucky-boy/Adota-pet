  </main>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer__grid">
        <div class="footer__col">
          <h3><span aria-hidden="true"><i data-lucide="sparkles"></i></span> Adota <em>Pet</em></h3>
          <p>Conectando pessoas e animais para uma história que começa em casa.</p>
        </div>
        <div class="footer__col">
          <h4>Links</h4>
          <ul>
            <li><a href="/">Início</a></li>
            <li><a href="/pets">Encontrar um pet</a></li>
            <li><a href="/register">Cadastre-se</a></li>
            <li><a href="/donate">Apoiar a causa</a></li>
            <li><a href="/volunteer">Ser voluntário</a></li>
            <li><a href="/login">Área ONG</a></li>
          </ul>
        </div>
        <div class="footer__col">
          <h4>Contato</h4>
          <p><i data-lucide="mail" aria-hidden="true"></i> contato@lovepatinhas.org</p>
          <p><i data-lucide="phone" aria-hidden="true"></i> (82) 99999-9999</p>
        </div>
      </div>
      <div class="footer__bottom">
        <p>&copy; 2026 Adota Pet — uma iniciativa da ONG Love Patinhas.</p>
      </div>
    </div>
  </footer>

  <script>
    // Mobile nav toggle
    const navToggle = document.getElementById('navToggle');
    if (navToggle) {
      navToggle.addEventListener('click', function() {
        document.querySelector('.navbar__links').classList.toggle('navbar__links--open');
        this.classList.toggle('navbar__toggle--active');
        var expanded = this.getAttribute('aria-expanded') === 'true';
        this.setAttribute('aria-expanded', String(!expanded));
        this.setAttribute('aria-label', expanded ? 'Abrir menu' : 'Fechar menu');
      });
    }

    if (window.lucide) {
      lucide.createIcons();
    }

    // Persistent light/dark theme toggle
    var themeToggle = document.getElementById('themeToggle');
    if (themeToggle) {
      var updateThemeToggle = function() {
        var darkMode = document.documentElement.dataset.theme === 'dark';
        themeToggle.setAttribute('aria-pressed', String(darkMode));
        themeToggle.setAttribute('aria-label', darkMode ? 'Ativar modo claro' : 'Ativar modo escuro');
        themeToggle.querySelector('.theme-toggle__label').textContent = darkMode ? 'Modo claro' : 'Modo escuro';
        var icon = themeToggle.querySelector('[data-lucide]');
        if (icon) icon.setAttribute('data-lucide', darkMode ? 'sun' : 'moon');
        if (window.lucide) lucide.createIcons();
      };

      themeToggle.addEventListener('click', function() {
        var nextTheme = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
        document.documentElement.dataset.theme = nextTheme;
        try { localStorage.setItem('adota-pet-theme', nextTheme); } catch (error) { /* Theme remains active for this page. */ }
        updateThemeToggle();
      });
      updateThemeToggle();
    }

    // Auto-dismiss alerts
    document.querySelectorAll('.alert').forEach(function(el) {
      setTimeout(function() { el.remove(); }, 5000);
    });
  </script>
</body>
</html>
